# Homebrew Tap for Stockyard Products
#
# Setup: This directory becomes the repo `stockyard/homebrew-tap`
# Users install via: brew install stockyard-dev/tap/costcap
#
# After each release, update the URLs and SHA256 hashes below.
# The release workflow dispatches an event to auto-update these.
