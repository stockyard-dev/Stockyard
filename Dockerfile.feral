FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/feral ./cmd/feral/

FROM alpine:3.19
COPY --from=build /bin/feral /usr/local/bin/feral
EXPOSE 9704
CMD ["feral"]
