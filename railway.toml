[build]
buildCommand = "CGO_ENABLED=0 go build -ldflags='-s -w -X main.version=v1.0.0' -o bin/stockyard ./cmd/stockyard/"

[deploy]
startCommand = "./bin/stockyard"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyMaxRetries = 3
