version: "3.7"
services:
  stockyard:
    image: ghcr.io/stockyard-dev/stockyard:latest
    restart: on-failure
    ports:
      - "${APP_STOCKYARD_PORT:-4200}:4200"
    environment:
      PORT: 4200
      DATA_DIR: /data
    volumes:
      - ${APP_DATA_DIR}/data:/data
