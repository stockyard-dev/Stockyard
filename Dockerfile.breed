FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/breed ./cmd/breed/

FROM alpine:3.19
COPY --from=build /bin/breed /usr/local/bin/breed
EXPOSE 9701
CMD ["breed"]
