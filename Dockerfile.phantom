FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/phantom ./cmd/phantom/

FROM alpine:3.19
COPY --from=build /bin/phantom /usr/local/bin/phantom
EXPOSE 9703
CMD ["phantom"]
