# ConsentGate — User consent management for AI.
# Copy to consentgate.yaml and set your API keys.

port: 6350
consentgate:
  enabled: true
  consent_header: X-User-Consent
  block_without_consent: true

# Common settings
data_dir: ~/.stockyard
log_level: info
