# StreamThrottle — Control streaming speed.
# Copy to streamthrottle.yaml and set your API keys.

port: 6390
streamthrottle:
  max_tokens_per_sec: 30
  buffer: true

# Common settings
data_dir: ~/.stockyard
log_level: info
