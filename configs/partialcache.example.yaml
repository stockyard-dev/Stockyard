# PartialCache — Cache reusable prompt prefixes.
# Copy to partialcache.yaml and set your API keys.

port: 6230
partialcache:
  enabled: true
  min_prefix_tokens: 100
  auto_detect: true

# Common settings
data_dir: ~/.stockyard
log_level: info
