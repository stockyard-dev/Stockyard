# SpotPrice — Real-time model pricing intelligence.
# Copy to spotprice.yaml and set your API keys.

port: 6300
spotprice:
  min_quality_score: 0.8
  update_interval: 1h
  prefer_cheapest: true

# Common settings
data_dir: ~/.stockyard
log_level: info
