# PromptMarket — Community prompt library.
# Copy to promptmarket.yaml and set your API keys.

port: 6270
promptmarket:
  enabled: true
  allow_publish: true
  require_rating: true

# Common settings
data_dir: ~/.stockyard
log_level: info
