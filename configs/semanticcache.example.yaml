# SemanticCache — Cache hits for similar prompts.
# Copy to semanticcache.yaml and set your API keys.

port: 6220
semanticcache:
  threshold: 0.92
  embed_model: text-embedding-3-small
  max_entries: 50000

# Common settings
data_dir: ~/.stockyard
log_level: info
