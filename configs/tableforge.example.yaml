# TableForge — LLM-powered table generation with validation.
# Copy to tableforge.yaml and set your API keys.

port: 6090
tableforge:
  expected_columns: [name, email, role]
  types: { name: string, email: email, role: string }
  require_complete: true

# Common settings
data_dir: ~/.stockyard
log_level: info
