# WebhookForge — Visual webhook-to-LLM pipeline builder.
# Copy to webhookforge.yaml and set your API keys.

port: 6640
webhookforge:
  enabled: true
  max_pipelines: 50
  execution_timeout: 60s

# Common settings
data_dir: ~/.stockyard
log_level: info
