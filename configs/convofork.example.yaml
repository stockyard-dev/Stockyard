# ConvoFork — Branch conversations. Try different paths.
# Copy to convofork.yaml and set your API keys.

port: 6200
convofork:
  max_forks_per_session: 10
  max_depth: 5

# Common settings
data_dir: ~/.stockyard
log_level: info
