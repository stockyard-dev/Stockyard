# EmbedRouter — Smart routing for embedding requests.
# Copy to embedrouter.yaml and set your API keys.

port: 6510
embedrouter:
  window_ms: 50
  deduplicate: true
  batch_size: 100

# Common settings
data_dir: ~/.stockyard
log_level: info
