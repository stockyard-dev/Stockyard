# GeoPrice — Purchasing power pricing by region.
# Copy to geoprice.yaml and set your API keys.

port: 6600
geoprice:
  multipliers:
    US: 1.0
    EU: 0.9
    BR: 0.4
    IN: 0.3
  detect_vpn: true

# Common settings
data_dir: ~/.stockyard
log_level: info
