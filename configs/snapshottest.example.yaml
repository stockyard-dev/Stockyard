# SnapshotTest — Snapshot testing for LLM outputs.
# Copy to snapshottest.yaml and set your API keys.

port: 6320
snapshottest:
  baseline_dir: ./snapshots/
  threshold: 0.85
  update_command: "snapshottest --update"

# Common settings
data_dir: ~/.stockyard
log_level: info
