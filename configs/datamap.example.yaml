# DataMap — GDPR Article 30 data flow mapping.
# Copy to datamap.yaml and set your API keys.

port: 6340
datamap:
  enabled: true
  classify_pii: true
  export_format: json

# Common settings
data_dir: ~/.stockyard
log_level: info
