# PlaybackStudio — Interactive playground for logged requests.
# Copy to playbackstudio.yaml and set your API keys.

port: 6630
playbackstudio:
  source: promptreplay
  search_index: true
  max_results: 1000

# Common settings
data_dir: ~/.stockyard
log_level: info
