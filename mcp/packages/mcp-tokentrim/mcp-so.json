{
  "name": "@stockyard/mcp-tokentrim",
  "title": "TokenTrim — Never hit a context limit again",
  "description": "Automatic context window management. Truncates prompts using middle-out, oldest-first, or newest-first strategies when they exceed model limits.",
  "install": "npx @stockyard/mcp-tokentrim",
  "config": {
    "command": "npx",
    "args": [
      "@stockyard/mcp-tokentrim"
    ],
    "env": {
      "OPENAI_API_KEY": "your-key"
    }
  },
  "tools_count": 5,
  "categories": [
    "llm",
    "proxy",
    "developer-tools"
  ]
}
