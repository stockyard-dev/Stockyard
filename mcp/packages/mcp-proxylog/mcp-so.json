{
  "name": "@stockyard/mcp-proxylog",
  "title": "ProxyLog — Structured logging for every proxy decision",
  "description": "Each middleware emits decision log. Per-request trace. X-Proxy-Trace header.",
  "install": "npx @stockyard/mcp-proxylog",
  "config": {
    "command": "npx",
    "args": [
      "@stockyard/mcp-proxylog"
    ],
    "env": {
      "OPENAI_API_KEY": "your-key"
    }
  },
  "tools_count": 3,
  "categories": [
    "llm",
    "proxy",
    "developer-tools"
  ]
}
