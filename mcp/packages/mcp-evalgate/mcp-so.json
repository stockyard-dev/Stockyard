{
  "name": "@stockyard/mcp-evalgate",
  "title": "EvalGate — Only ship quality LLM responses",
  "description": "Response quality scoring and auto-retry. Validate JSON, check length, match regex, run custom expressions. Auto-retry on failure.",
  "install": "npx @stockyard/mcp-evalgate",
  "config": {
    "command": "npx",
    "args": [
      "@stockyard/mcp-evalgate"
    ],
    "env": {
      "OPENAI_API_KEY": "your-key"
    }
  },
  "tools_count": 5,
  "categories": [
    "llm",
    "proxy",
    "developer-tools"
  ]
}
