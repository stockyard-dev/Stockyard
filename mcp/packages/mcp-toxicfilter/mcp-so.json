{
  "name": "@stockyard/mcp-toxicfilter",
  "title": "ToxicFilter — Content moderation for LLM outputs",
  "description": "Content moderation middleware for LLM responses. Block, redact, or flag harmful, hateful, or unsafe content before it reaches users.",
  "install": "npx @stockyard/mcp-toxicfilter",
  "config": {
    "command": "npx",
    "args": [
      "@stockyard/mcp-toxicfilter"
    ],
    "env": {
      "OPENAI_API_KEY": "your-key"
    }
  },
  "tools_count": 5,
  "categories": [
    "llm",
    "proxy",
    "developer-tools"
  ]
}
