name: stockyard-cronllm
display_name: "Stockyard CronLLM"
description: "Define scheduled prompts in YAML. Daily summaries, weekly reports, periodic checks. Runs through full proxy chain."
icon: "⏰"
command: npx
args:
  - "@stockyard/mcp-cronllm"
env:
  OPENAI_API_KEY:
    description: "OpenAI API key"
    required: true
    secret: true
tags:
  - cron
  - schedule
  - automation
  - tasks
  - periodic
