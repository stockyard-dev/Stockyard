FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/cortex ./cmd/cortex/

FROM alpine:3.19
COPY --from=build /bin/cortex /usr/local/bin/cortex
EXPOSE 9707
CMD ["cortex"]
