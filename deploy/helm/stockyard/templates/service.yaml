apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-stockyard
  labels:
    app: stockyard
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 7749
      protocol: TCP
      name: http
  selector:
    app: stockyard
    release: {{ .Release.Name }}
